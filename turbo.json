{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "../../dist/apps/ui/**"]
    },
    "dev": {
      "cache": false
    },
    "lint": {}
  },

  "globalDependencies": ["tsconfig.json"]
}
