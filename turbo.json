{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "../../dist/apps/ui/**"]
    },
    "dev": {
      "dependsOn": ["^dev"]
    },
    "lint": {}
  },

  "globalDependencies": ["tsconfig.json"]
}
